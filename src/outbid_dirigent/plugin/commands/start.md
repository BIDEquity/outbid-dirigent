---
name: start
description: Alias for /dirigent:hi — the Dirigent coach and daily-driver entry point
argument-hint: [optional: what do you want to build?]
allowed-tools: Read, Bash, Glob, Grep, Edit, Write, Skill
---

Alias for `/dirigent:hi`. Invokes the `hi` skill with the same behavior. Use whichever name feels natural.

Use the Skill tool to invoke `hi` with `$ARGUMENTS` as the skill input.
