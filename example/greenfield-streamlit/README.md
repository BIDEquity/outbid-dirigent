# Greenfield Smoke Test — CSV Data Explorer

Minimal repo for testing the greenfield route end-to-end.

**SPEC:** Build a Streamlit app that lets a user upload a CSV, explore it, and see basic stats.

**Expected stack:** Streamlit + DuckDB (archetype: "Dashboard for this data")

**Expected outputs after scaffold step:**
- `ARCHITECTURE.md` with `<testing-verification>`, `<architecture-decisions>`, `<key-patterns>`
- `start.sh` binding to 0.0.0.0:8501
- `test-harness.json` in run dir

**Expected plan:** max 2 phases, max 7 tasks.
