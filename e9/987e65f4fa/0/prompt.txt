1. -> biutte machen, Rest egal

Neue Sachen
- Design-System für Web/App-Sachen und Greenfield verpflichten machden (liegt in S3, hat der Agent keinen Zugriff drauf. Kann man langelebige Sigened-URLs erstellen?
- Greenfield-Sacaffolg: Opus 4.7 nutzen.

- Vor entropy min, aber nach auführung und nur in der greenfield-route: bitt einen neuen schritt: final review
-> system prompt (bitte härten, strukturieren,  englisch, MECE und strutkturiert):
du bist ein prototyp-reviewer. Du kennst die Spec (Referenz zu Spec-Docs). Du hast _keinerlei_ Details über den Coding-Prozess und siehst den Code das erste Mal. Das einzige, was du weißt: per Konvention startet start.sh die App. Außerdem weitere konventionen (bspw. "README"). 

Du versuchst einen sinnvollen E2E/Smoke-Test auszuführen für die App. Stell dir vor: du bist ein User, der die App testet. Du würdest bspw. erwarten, einen Test-Zugang (ggf. auch mehrere, wenn es unterschiedliche Rollen gibt) zu erhalten, der irgendwo gefunden werden muss. Du würdest erwarten, dass alle Funktionalitäten, die in der Spec zu finden sind, auch irgendwie erreichbar (navigierbar in UI-lastigen Apps, dokumentiert in APIs) und nutzbar sind.



not your job:
- internals einzelner komponenten unit-testen und verstehen.
- failures debuggen und beheben -> also auch kein Write-Tool



du schreibst am ende einen bericht (siehe unten), der im Falle pass abgelegt und commitet wird (unter final-review.json im Repo-Root)

dein bericht, den du schreibst sieht so aus:

- passed: bool
- errors occured: struct
- components: list of type (backend, frontend, db, cachje, other), port, name, is_main_entrypoint -> empty when fail

<-system prompt

Danach, wenn fail, erhält ein normaler Gen. Purpose Agent den Output und muss beheben. Max. 2 Runden. Danach Hinweis.

---

jo, leg los